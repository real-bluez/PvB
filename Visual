-- LOCALS --
local RobloxMenu = game:GetService("CoreGui").RobloxGui
local Leaderboard = game:GetService("CoreGui").PlayerList
local Gui = game:GetService("Players").LocalPlayer.PlayerGui
local players = game:GetService("Players")
local currentPlayers = #players:GetPlayers()
local maxPlayers = players.MaxPlayers
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local function CheckEquipedTool()
	local Character = players.LocalPlayer.Character
	if Character then
		local EquippedTool = Character:FindFirstChildWhichIsA("Tool")
		if EquippedTool then
			print("Equipped Tool: " .. EquippedTool.Name)
			return EquippedTool
		else
			print("Nothing Equipped.")
			return nil
		end
	end
	return nil
end

CheckEquipedTool()

-- INSTANCES --
local Main = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local MainButton = Instance.new("TextButton")
local BypassButton = Instance.new("TextButton")
local ByButtonStrokeIn = Instance.new("UIStroke")
local ByButtonStrokeOut = Instance.new("UIStroke")
local ByButtonCorner = Instance.new("UICorner")
local Corner = Instance.new("UICorner")
local ButtonCorner = Instance.new("UICorner")
local Gradient = Instance.new("UIGradient")
local Stroke = Instance.new("UIStroke")
local ButtonStrokeIn = Instance.new("UIStroke")
local ButtonStrokeOut = Instance.new("UIStroke")
local Title = Instance.new("TextLabel")
local Subtitle = Instance.new("TextLabel")
local TitleStroke = Instance.new("UIStroke")
local SubTitleStroke = Instance.new("UIStroke")
local BySubtitle = Instance.new("TextLabel")
local BySubTitleStroke = Instance.new("UIStroke")

-- SETUP FOR INSTANCES --
Gradient.Rotation = 90
Title.Parent = Frame
Title.TextScaled = true
Title.BackgroundTransparency = 1
Title.TextColor3 = Color3.new(1, 1, 1)
Title.Position = UDim2.new(0, 10, 0, -1)
Title.Text = "Tool Script's Hub"
Title.Size = UDim2.new(0, 278, 0, 60)
Subtitle.Parent = Frame
Subtitle.TextScaled = false
Subtitle.BackgroundTransparency = 1
Subtitle.TextColor3 = Color3.new(1, 1, 1)
Subtitle.Position = UDim2.new(0, 10, 0, 45)
Subtitle.Text = "Holding: None"
Subtitle.Size = UDim2.new(0, 278, 0, 40)
Subtitle.TextSize = 15
SubTitleStroke.Parent = Subtitle
SubTitleStroke.Thickness = 3
BySubtitle.Parent = Frame
BySubtitle.TextScaled = false
BySubtitle.BackgroundTransparency = 1
BySubtitle.TextColor3 = Color3.new(1, 1, 1)
BySubtitle.Position = UDim2.new(0, 10, 0, 75)
BySubtitle.Text = "Bypass: No ❌"
BySubtitle.Size = UDim2.new(0, 278, 0, 40)
BySubtitle.TextSize = 15
BySubTitleStroke.Parent = BySubtitle
BySubTitleStroke.Thickness = 3
TitleStroke.Parent = Title
TitleStroke.Thickness = 3
ButtonStrokeIn.Parent = MainButton
ButtonStrokeIn.Thickness = 2
ButtonStrokeIn.ApplyStrokeMode = "Contextual"
ButtonStrokeOut.Parent = MainButton
ButtonStrokeOut.Thickness = 3
ButtonStrokeOut.ApplyStrokeMode = "Border"
Stroke.Parent = Frame
Stroke.Thickness = 3
Gradient.Parent = Frame
Gradient.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0, Color3.fromRGB(58, 94, 255)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0)),
})
ButtonCorner.Parent = MainButton
ButtonCorner.CornerRadius = UDim.new(0, 9)
Corner.Parent = Frame
Corner.CornerRadius = UDim.new(0, 9)
Main.Name = "ToolScriptsDuper"
Main.ResetOnSpawn = false
Frame.Size = UDim2.new(0, 300, 0, 190)
Frame.Position = UDim2.new(0, 1300, 1, -300)
Main.Parent = Gui
Frame.Parent = Main
MainButton.Parent = Frame
MainButton.Size = UDim2.new(0, 286, 0, 30)
MainButton.Position = UDim2.new(0, 7, -0, 153)
MainButton.TextScaled = true
MainButton.Text = "Start⏯️"
MainButton.BackgroundColor3 = Color3.new(0, 0.133333, 0.380392)
MainButton.TextColor3 = Color3.new(1, 1, 1)
BypassButton.Parent = Frame
BypassButton.Size = UDim2.new(0, 286, 0, 30)
BypassButton.Position = UDim2.new(0, 7, -0, 113)
BypassButton.TextScaled = true
BypassButton.Text = "Bypass⛓️‍💥"
BypassButton.BackgroundColor3 = Color3.new(0, 0.133333, 0.380392)
BypassButton.TextColor3 = Color3.new(1, 1, 1)
ByButtonCorner.Parent = BypassButton
ByButtonCorner.CornerRadius = UDim.new(0, 9)
ByButtonStrokeIn.Parent = BypassButton
ByButtonStrokeIn.Thickness = 2
ByButtonStrokeIn.ApplyStrokeMode = "Contextual"
ByButtonStrokeOut.Parent = BypassButton
ByButtonStrokeOut.Thickness = 3
ByButtonStrokeOut.ApplyStrokeMode = "Border"
Frame.Active = true
Frame.Draggable = true
_G.Bypass = false

local function updategui()
	local Holding = CheckEquipedTool()
	if Holding then
		Subtitle.Text = "Holding: " .. Holding.Name
	else
		Subtitle.Text = "Holding: None "
	end
end

players.LocalPlayer.Character.ChildAdded:Connect(updategui)
players.LocalPlayer.Character.ChildRemoved:Connect(updategui)

MainButton.MouseButton1Click:Connect(function()
	if _G.Bypass == true then
		local HoldingTool = CheckEquipedTool()
		if HoldingTool then
			local clonedtool = HoldingTool:Clone()
			clonedtool.Parent = players.LocalPlayer.Backpack
			game:GetService("StarterGui"):SetCore("SendNotification", {
				Title = "Tool Script's Hub ✅", -- Requerido
				Text = "You duped correctly enjoy!",
			})
		else
			game:GetService("StarterGui"):SetCore("SendNotification", {
				Title = "Tool Script's Hub ⚠️", -- Requerido
				Text = "You need to be holding something to dupe!",
			})
			warn("You Need to hold something!")
		end
	else
		game:GetService("StarterGui"):SetCore("SendNotification", {
			Title = "Tool Script's Hub ⚠️", -- Requerido
			Text = "You need to bypass first!",
		})
	end
end)

BypassButton.MouseButton1Click:Connect(function()
	if _G.Bypass == false then
		_G.Bypass = true
		local waitingtime = math.random(3, 7)
		game:GetService("StarterGui"):SetCore("SendNotification", {
			Title = "Tool Script's Hub ⚠️", -- Requerido
			Text = "We are bypassing, please wait!",
		})
		task.wait(waitingtime)
		BySubtitle.Text = "Bypass: Done ✅"
		game:GetService("StarterGui"):SetCore("SendNotification", {
			Title = "Tool Script's Hub ✅", -- Requerido
			Text = "We bypassed, now you can generate!",
		})
	else
		game:GetService("StarterGui"):SetCore("SendNotification", {
			Title = "Tool Script's Hub ❌", -- Requerido
			Text = "We already bypassed!",
		})
	end
end)
